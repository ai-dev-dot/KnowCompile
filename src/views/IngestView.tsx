interface Props { kbPath: string }
export default function IngestView({ kbPath: _kbPath }: Props) {
  return (
    <div className="flex-1 flex items-center justify-center">
      <p className="text-text-muted">资料摄入（即将实现）</p>
    </div>
  )
}
