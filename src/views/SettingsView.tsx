interface Props { kbPath: string }
export default function SettingsView({ kbPath: _kbPath }: Props) {
  return (
    <div className="flex-1 flex items-center justify-center">
      <p className="text-text-muted">设置（即将实现）</p>
    </div>
  )
}
